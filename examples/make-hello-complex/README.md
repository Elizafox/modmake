# Complex Make example

This project exercises a primary module interface, an exported partition in a
nested directory, a module implementation unit, and an ordinary importing
translation unit. From this directory:

```sh
make check CXX=clang++
make clean
make check CXX=g++
```
